Public-domain plain text from the Project Gutenberg mirror at
gutenberg.pglaf.org. License wrappers are stripped so the page can load
excerpts from the same origin (GitHub Pages cannot fetch gutenberg.org
because it does not send CORS headers).

Refresh the files after catalog changes:

```bash
python3 scripts/fetch_gutenberg_texts.py
```
